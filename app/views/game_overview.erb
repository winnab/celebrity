<h2>Game Details</h2>
<p>Welcome <%= @name %>! Here is everyone who has joined the game so far.</p>

<h4>Players</h4>
<ul>
  <li><%= @name %></li>
  <% if @players %>
    <li><%= @players.join(", ") %> <em>not joined</em></li>
  <% end %>
</ul>

<h4>Invite Players</h4>
<form
  method="post"
  accept-charset="utf-8"
  action="/invite"
>
  <label
    for="invite_email">
    Invite player:
    <input type="text" name="invite_email">
  </label>
  <button type="submit">Invite Player</button>
</form>
